# Игнорировать виртуальную среду
venv/
.env

# Игнорировать файлы конфигурации и временные файлы
= 
=20.0
ERROR
README.md

# Игнорировать requirements.txt, если он временный
requirements.txt

# Игнорировать любые файлы с sha256 хешами (если есть специально)
# Обычно такие строки не добавляют в репозиторий, но если есть файлы с ними, добавьте:
# *.sha256

# Дополнительно (по желанию) — игнорировать логи или системные файлы
*.log
.DS_Store