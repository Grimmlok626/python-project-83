[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hexlet-code"
version = "0.1.0"
dependencies = [
    "flask",
    "gunicorn",
    "python-dotenv",
    "psycopg2-binary"
]

[tool.hatch.build.targets.wheel]
packages = ["page_analyzer"]